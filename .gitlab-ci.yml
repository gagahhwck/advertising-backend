stages:
  - deploy

deploy-job:
  stage: deploy
  script:
    - cd /home/gagah/public_html/advertising-be
    - git stash
    - git pull
    - docker compose up -d
    - docker exec advertising bash -c 'composer update'
    - docker exec advertising bash -c 'php artisan migrate'
    - docker compose restart
  tags:
    - runner-api-dev
